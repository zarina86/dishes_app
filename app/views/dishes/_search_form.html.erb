<div class= "col-md-12 mb-3 mt-3">
  <%= search_form_for @search do |f| %>
    <% Ingredient.all.each do |ingredient|%>
      <%= check_box_tag('q[ingredients_id_not_eq_any][]', ingredient.id) %>
        <%= ingredient.name %>
      <% end %>
          
      </div>
        <div class="actions mb-3"><%= f.submit "Search", class: "btn btn-success" %></div>
  <% end %>
</div>