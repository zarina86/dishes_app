<%= form_with(model: ingredient) do |form| %>
  <% if ingredient.errors.any? %>
    <div style="color: red">
      <h6><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h6>

      <ul>
        <% ingredient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row mb-3 mt-3">
    <%= form.label :name %>
    <div class="col-sm-6 col-md-6">
      <%= form.text_field :name, class: "form-control" %>
    </div>
  </div>

  <div>
    <%= form.submit "Submit", class: "btn btn-success" %>
    <%= link_to "Back", ingredients_path, class: "btn btn-primary" %>
  </div>
<% end %>
